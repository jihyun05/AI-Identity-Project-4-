models_config: config/models.yaml

persona: config/personas/worker_lee_grounded_avatar.yaml

scenarios:
  - config/scenarios/visual_consistency.yaml

target_models:
  - qwen3-vl-4b

evaluators:
  - visual_consistency

evaluator_kwargs:
  visual_consistency:
    reference_avatar_path: assets/personas/worker_lee.png

repeats: 5

output_dir: results_new/visual_portrait/worker_lee_qwen