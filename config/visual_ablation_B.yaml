models_config: config/models.yaml

persona: config/personas/worker_lee_grounded_avatar_v2.yaml

scenarios:
  - config/scenarios/visual_consistency.yaml

target_models:
  - gpt-4o-mini

evaluators:
  - visual_consistency

evaluator_kwargs:
  visual_consistency:
    reference_avatar_path: assets/personas/worker_lee.png

reminder: true

repeats: 5

output_dir: results_new/visual_portrait_vpa/worker_lee_grounded_avatar_v2